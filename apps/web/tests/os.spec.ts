import { test, expect } from '@playwright/test';
test('opens App Shop and installs a visible placeholder app', async ({ page }) => { const name = `Tetris ${Date.now()}`; await page.goto('/'); await page.getByTitle('App Shop').click(); await expect(page.getByRole('heading', { name: 'App Shop' })).toBeVisible(); await page.getByLabel('Search apps').fill(name); await page.getByRole('button', { name: 'Imagine and install' }).click(); await expect(page.getByText(`${name} · placeholder`)).toBeVisible(); await page.getByRole('button', { name: 'Close window' }).click(); await page.locator('.launcher-button').click(); await expect(page.getByText(name)).toBeVisible(); });
