# App memory

- Stable app identity: `app-plants-vs-zombies`; title is `Plants Vs Zombies`.
- The launch surface is a self-contained playable garden-defense canvas in `index.html`.
- Persistent state is intentionally minimal: best score is stored under `garden-progress`; live game state remains local to the surface.
